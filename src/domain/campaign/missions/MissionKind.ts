/** Tipos de missão do board (camp-missions). */
export type MissionKind = 'main' | 'side' | 'normal';

/** Dificuldade das missões normais (template por mapa × estrela). */
export type MissionStars = 1 | 2 | 3 | 4 | 5;
